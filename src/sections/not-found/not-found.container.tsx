import * as React from 'react';
import NotFoundView from './not-found.view';

const NotFoundContainer: React.FunctionComponent = () => {
    return <NotFoundView message="URL not found!" />
}

export default NotFoundContainer;